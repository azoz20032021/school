# النشر والاستضافة

## الخلاصة أولاً

**ابقَ على Vercel.** لا تحتاج Azure ولا سيرفر خاص.

مدرسة بحجم 500–1500 طالب هي حِمل صغير جداً على أي استضافة حديثة. الذروة عندك
تكون لحظة إعلان النتائج — يعني ربما 300 طالب يفتحون الموقع خلال دقيقتين. هذا
لا يقارب حدود الخطة المجانية.

**المشكلة عندك لم تكن الاستضافة، كانت عدد استعلامات Firestore لكل طلب.**
صفحة واحدة لصف فيه 30 طالب كانت تُطلق أكثر من 60 استعلاماً متسلسلاً. هذا
أُصلح — صار 3 استعلامات مهما كان عدد الطلاب. تغيير الاستضافة ما كان راح يحل
هذا؛ كان راح يخلي نفس البطء بفاتورة أعلى.

---

## المقارنة

| الخيار | الكلفة الشهرية | الجهد | مناسب لك؟ |
|---|---|---|---|
| **Vercel Hobby** | مجاني | لا شيء | ✅ ابدأ هنا |
| **Vercel Pro** | ~$20 | لا شيء | ✅ إذا تجاوزت حدود المجاني أو أردت تثبيت المنطقة |
| Railway / Render | ~$5–10 | قليل | بديل جيد، خادم دائم بدون cold start |
| VPS (Hetzner) | ~€5 | عالٍ | تصير أنت مسؤول التحديثات والنسخ الاحتياطي و SSL |
| **Azure** | $$$ | عالٍ جداً | ❌ مصمم لأنظمة مؤسسات ضخمة |

Azure ليس "أفضل"، هو **أكبر**. تدفع مقابل مرونة مؤسسية (شبكات، صلاحيات، امتثال)
لا تحتاجها مدرسة، وتدفعها بالمال وبوقتك في الإدارة. لو انتقلت له اليوم راح تقضي
أسابيع في الإعداد وتحصل على نفس السرعة.

---

## الأهم من اختيار المزود: موقع قاعدة البيانات

هذي النقطة تفرق أكثر من أي شيء آخر.

كل طلب على الـ API يعمل عدة استعلامات على Firestore. لو كانت الدالة في
فرانكفورت وقاعدة البيانات في أمريكا، كل استعلام يعبر المحيط. المستخدم يدفع
هذا التأخير **مضروباً في عدد الاستعلامات**، بينما المسافة بين الطالب والخادم
تُدفع مرة واحدة فقط.

**القاعدة: ضع الدالة في نفس منطقة Firestore، وليس قرب الطلاب.**

1. اعرف منطقة قاعدتك:
   `Firebase Console → Firestore Database` — المنطقة مكتوبة أعلى الصفحة.
2. ثبّت منطقة Vercel لتطابقها بإضافة `regions` في `vercel.json`:

| منطقة Firestore | ضع في vercel.json |
|---|---|
| `eur3` / `europe-west*` | `"regions": ["fra1"]` |
| `nam5` / `us-central1` | `"regions": ["iad1"]` |
| `asia-south1` | `"regions": ["bom1"]` |

```json
{
  "regions": ["fra1"],
  "rewrites": [ ... ]
}
```

> منطقة Firestore **تُحدد عند الإنشاء ولا يمكن تغييرها**. إذا طلعت في أمريكا
> وأغلب طلابك في العراق، الخيار الأفضل هو `iad1` (مع قاعدة البيانات) — أو
> إنشاء مشروع Firebase جديد في `eur3` ونقل البيانات إليه، وهذا يستحق العناء
> فقط لو صار التأخير ملموساً.

---

## إقفال قاعدة البيانات

إعدادات Firebase الظاهرة في الواجهة ليست سراً — أي شخص يقدر يأخذها من المتصفح.
الشيء الوحيد الذي يمنعه من قراءة قاعدة البيانات مباشرة هو `firestore.rules`.

الفكرة: **لا أحد يتصل بـ Firestore مباشرة.** كل وصول يمر عبر الـ API، والـ API
يسجّل دخوله بحساب خدمة واحد مخصص، والقواعد ترفض أي هوية غيره.

### الخطوات (مرة واحدة)

1. `Firebase Console → Authentication → Sign-in method` وفعّل **Email/Password**.

2. `Authentication → Users → Add user` — استخدم بريداً تملكه، مثلاً
   `server@almaali.local`، وكلمة مرور طويلة وعشوائية:

   ```bash
   node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
   ```

3. انسخ الـ **UID** الخاص بهذا المستخدم، وضعه في `firestore.rules` مكان
   `REPLACE_WITH_SERVER_ACCOUNT_UID`.

4. ضع نفس البيانات في متغيرات البيئة على Vercel:

   ```
   FIREBASE_SERVER_EMAIL=server@almaali.local
   FIREBASE_SERVER_PASSWORD=<كلمة المرور الطويلة>
   ```

5. انشر القواعد:

   ```bash
   firebase deploy --only firestore:rules
   ```

> **انشر القواعد ومتغيرات البيئة معاً.** إذا نشرت القواعد قبل ضبط المتغيرات،
> ما راح يقدر الـ API يوصل لقاعدة البيانات وراح ترجع الطلبات خطأ 503.

### التأكد أن الإقفال نجح

افتح الموقع وسجّل دخول (يجب أن تعمل البيانات طبيعياً)، ثم افتح console المتصفح
وجرّب قراءة مباشرة من Firestore — يجب أن تفشل بـ:

```
Missing or insufficient permissions
```

إذا نجحت القراءة، معناها القواعد لم تُنشر بعد.

### إذا صار خطأ بعد نشر القواعد

| الرسالة | السبب | الحل |
|---|---|---|
| `الخادم غير مصرح له بالوصول لقاعدة البيانات` (503) | القواعد منشورة بس `FIREBASE_SERVER_EMAIL` / `FIREBASE_SERVER_PASSWORD` غير مضبوطة، أو الـ UID داخل `firestore.rules` مختلف عن حساب الخدمة | اضبط المتغيرين على Vercel ثم **Redeploy**، وتأكد أن الـ UID مطابق |
| `تعذر الاتصال بقاعدة البيانات` (503) | البريد أو كلمة المرور غلط (السجل يبيّن `auth/invalid-credential`) | صحّح البيانات، أو أعد تعيين كلمة مرور حساب الخدمة |
| `تعذر إتمام الطلب (500)` | الدالة نفسها طاحت | راجع سجل Vercel: `Deployments → آخر نشر → Functions → Logs` |

كل رسالة 503 يقابلها سطر مفصّل في سجل الخادم يشرح السبب بالضبط.

---

## خطوات النشر على Vercel

1. اربط المستودع بـ Vercel (موجود أصلاً).
2. `Project Settings → Environment Variables` وأضف:

   | المتغير | ملاحظة |
   |---|---|
   | `SESSION_SECRET` | إلزامي |
   | `FIREBASE_SERVER_EMAIL` | إلزامي لإقفال قاعدة البيانات |
   | `FIREBASE_SERVER_PASSWORD` | إلزامي |
   | `SETUP_TOKEN` | اختياري |

3. نفّذ خطوات "إقفال قاعدة البيانات" أعلاه.
4. أنشئ حساب المدير عبر `POST /api/setup`.

---

## ماذا لو كبر النظام فعلاً؟

راقب هذين الرقمين فقط:

- **قراءات Firestore/اليوم** في Firebase Console. الحد المجاني 50,000 قراءة يومياً.
- **مدة تنفيذ الدوال** في Vercel.

إذا اقتربت من حد القراءات، الحل ليس استضافة أكبر، بل:

1. زيادة مدة الكاش في `backend/lib/cache.ts` للقوائم البطيئة التغيّر.
2. تخزين العدادات المحسوبة (عدد الغيابات، مجموع المستحقات) كحقول جاهزة بدل
   حسابها في كل طلب.
3. الانتقال إلى `firebase-admin` مع خادم دائم (Railway/VPS) للاستفادة من
   الاتصال المستمر بدل إعادة المصادقة في كل cold start.

---

## نقاط أمنية يجب متابعتها بعد النشر

- [ ] `SESSION_SECRET` مضبوط وطويل، وغير موجود في Git.
- [ ] قواعد Firestore منشورة، والقراءة المباشرة من المتصفح تفشل.
- [ ] تم تغيير كلمة مرور حساب المدير القديم `ADM001` (كانت `admin123`).
      كلمات المرور القديمة تُحوّل تلقائياً إلى صيغة مشفّرة عند أول تسجيل دخول،
      لكن الكلمة نفسها ما زالت معروفة — غيّرها من صفحة الإعدادات.
- [ ] `deploy_error*.txt` و `school.db` و `server.bak.ts` محذوفة من المستودع
      (مضافة الآن إلى `.gitignore` لكنها ما زالت في تاريخ Git).
